{
  "name": "unbinge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Manifest V3 Chrome extension for a distraction-free, whitelist-based educational YouTube experience.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3",
    "vite": "^5.3.4"
  }
}
